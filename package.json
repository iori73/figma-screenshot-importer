{
  "name": "screenshot-importer",
  "version": "2.0.0",
  "description": "Import screenshots into Figma with folder structure preservation and automatic smart grouping for smartphone app screenshots",
  "scripts": {
    "build": "node build.mjs",
    "build:legacy": "tsc",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.2",
    "esbuild": "^0.27.3",
    "typescript": "^5.3.3"
  }
}
