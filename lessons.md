# Lessons Learned

## Figma Plugin Development

### Figmaサンドボックスの制約
- `atob()` が使えない → カスタム base64 デコーダーが必要
- `fetch()` は `manifest.json` の `networkAccess.allowedDomains` で許可されたドメインのみ
- `networkAccess: ["none"]` の場合、外部APIは一切使えない

### manifest.json の networkAccess は UI iframe にも適用される
- Gemini API を UI の `fetch()` で呼び出す場合でも、`manifest.json` に許可ドメインを記載しなければブロックされる
- `allowedDomains` に正確な URL（`https://generativelanguage.googleapis.com`）を指定する必要がある
- AI機能を追加したら manifest の更新を忘れずに確認する

### null-origin iframe でのドラッグ＆ドロップ制約
- Figma プラグイン UI は null-origin iframe で動作するため、OS からの**フォルダドロップで中身を読み取ることは不可能**
- `webkitGetAsEntry()` でディレクトリエントリは取得できるが、`readEntries()` は `EncodingError`（Data URL 長制限）で失敗する
- `getAsFileSystemHandle()` や `for await` は Figma の JS VM でクラッシュを引き起こす
- `document.addEventListener('dragover/drop', ...)` は Figma 本体のドラッグ処理を破壊してクラッシュさせるため**絶対に使わない**
- 安全なアプローチ: ドロップゾーン要素のみにイベントリスナーを設定し、`dataTransfer.items` + `item.getAsFile()` で個別ファイルのみ処理
- フォルダ構造付きインポートは `<input webkitdirectory>` のクリック選択でのみ実現可能

### メモリ管理
- `URL.createObjectURL()` を使ったら必ず `URL.revokeObjectURL()` で解放する
- 100枚以上の画像処理でメモリリークが顕著になる
- 画像処理後は canvas への参照を残さないようにする

### UIスレッドブロッキング
- ピクセル単位のネストループは50枚以上の画像でUIをフリーズさせる
- `await new Promise(r => setTimeout(r, 0))` をループの5回ごとに入れることで、UIスレッドに制御を返せる
- 重い処理は Web Worker が理想だが、Figma Plugin UI では制約がある

### `img.onerror` の重要性
- `img.onload` だけでは、壊れた画像で Promise が永久に resolve しない
- `onerror` ハンドラは必ずセットで実装する

## コード品質

### innerHTML と XSS
- ファイル名をそのまま innerHTML に挿入すると、`<script>` や `"` で HTML が壊れる
- `escapeHtml()` ユーティリティを共通関数として用意し、全ての動的文字列に適用
- Figma プラグインは外部ユーザー入力が少ないが、ファイル名は信頼できない入力

### エラーハンドリングの粒度
- `figma.createImage()` は不正なバイト列でクラッシュする可能性がある
- 画像単位で try-catch し、失敗した画像をスキップしつつ処理を継続する設計が重要
- スキップされた画像数をユーザーに通知する

## Smart Import アルゴリズム

### ピクセル比較のアプローチ
- 120px 幅へのダウンスケールで十分な精度が出る（パフォーマンスと精度のバランス）
- スクロール検出: 下部15%ストリップと上部65%領域のスライディングウィンドウマッチング
- ステータスバー（上部6%）とナビバー（下部12%）を除外することで誤検出を削減

### 単色ストリップ問題と分散チェック
- ダーク系UIアプリ（例: GO Taxi）では、画面下部がほぼ単色の濃紺になる
- 単色ストリップ同士の比較は、コンテンツが異なっても高い類似度を返す → スクロール誤検出
- `stripHasEnoughDetail`: ストリップのピクセル分散（variance）を計算し、閾値未満ならスクロール判定をスキップ
- variance < 80 をほぼ単色と判定する基準として採用

### セクション検出の工夫
- ナビバー領域（下部13%）の類似度だけでは不十分
- 全体色分布（10行サンプリング）との組み合わせで精度向上
- 閾値はユーザーがスライダーで調整可能にすることが重要（万能な値は存在しない）

## AI 機能の導入と撤退（v3.1.0 → v4.0.0）

> AI コードは `src/archive/gemini-ai.ts` にアーカイブ済み。以下は将来の再導入判断のための記録。

### 外部 API 依存の根本的リスク
- **レート制限**: Gemini 無料枠（1日1,500リクエスト）は数値上は十分だが、画像を複数枚送る1リクエストで 429 エラーが頻発した
- **デバッグの困難さ**: API エラーが Figma iframe 内で発生すると、エラー表示が一瞬で消えたり、minified コードでスタックトレースが追えない
- **外部依存 = 壊れやすさ**: API のレスポンス形式変更、レート制限ポリシー変更、サービス停止など、プラグイン開発者がコントロールできない要因が多すぎる

### BYOK パターンの UX 課題
- ユーザーに API キーの取得・入力を要求すること自体が大きな摩擦
- API キーの入力タイミング問題: 画像選択後にしか設定 UI が出ないと、ワークフローが中断される
- `localStorage` での永続化は iframe リロードで消える場合があり、毎回入力が必要になるケースも
- 「無料で使える」と宣伝しても、Google Cloud Console でキー発行する手順が非テック系ユーザーにはハードル

### 付加価値 vs コア体験のトレードオフ
- AI ラベリングは「あれば便利」だが、なくてもプラグインの核心価値（フォルダ構造→Figma セクション変換、スクロール検出）は完全に機能する
- AI 機能のデバッグと改善に費やした時間が、コア機能の改善に使えた時間を大幅に上回った
- 付加価値機能がコア体験の信頼性を損なうなら、**削除してミニマルにする勇気が必要**

### 技術的な学び（アーカイブ向け）
- BYOK で `placeholder` vs `value`: value にデフォルト設定し、ユーザーが必要な場合だけ編集する設計が良い
- サムネイルサイズ: 48px JPEG で Gemini Flash は十分識別可能だが、200px の方が精度は高い
- Index マッピング: AI レスポンスと元画像のインデックスずれを `Map<number, string>` で解決
- リトライ: 429 に対する指数バックオフリトライは実装したが、無料枠の根本的制限は解消できない

### 将来の再導入条件
- ローカル推論（WebAssembly / on-device モデル）が Figma iframe 内で動作可能になった場合
- Figma 公式が AI 命名 API を提供した場合
- サーバーサイドプロキシを用意し、ユーザーに API キー管理を不要にできる場合
- 再導入時は `src/archive/gemini-ai.ts` のコメントに復元手順を記載済み

## UX

### タブ vs 単一フロー
- ユーザーの目的が「画像をFigmaに整理してインポート」の1つなのに、2つのタブで「モード選択」を迫るのは認知負荷が高い
- 入力の種類（フォルダ vs 個別ファイル）で自動的に最適な処理を走らせる方がフリクションが少ない
- `webkitRelativePath` のパス深さ（pathParts.length >= 3）でサブフォルダ有無を判定し、自動分岐させるのがシンプルで確実

### 設定の折りたたみ
- ほとんどのユーザーはデフォルト設定で十分。常に全設定が表示されているのは画面を圧迫する
- `<details>` + `<summary>` でネイティブアコーディオンにすると、追加JSが不要でアクセシビリティも良い
- 詳細設定（Scroll/Section Sensitivity）は自動解析モード時のみ表示にすることで、不要な選択肢を隠せる

### 初見ユーザーへの配慮
- 「Scroll Sensitivity」「Section Sensitivity」は説明なしでは意味不明
- スライダーの下に短いヒントテキスト（"Higher = stricter scroll matching"）を追加するだけで理解度が大きく向上
- ドロップゾーンの状態変化（has-files クラス）でファイル選択済みであることを明示するとユーザー安心感が増す

### 言語切替の明示性
- 単一ボタン（「JA」とだけ表示）は何のことかわからない
- セグメントコントロール（`[EN] [JA]`）にすると、両方の選択肢が常に見えて一目で理解できる

### ダークモード
- Figma プラグイン UI は Figma のテーマに応じて `prefers-color-scheme` が変わる
- CSS カスタムプロパティを使えば、1つの `@media` ブロックで全色切り替え可能
- ハードコードされた色値をすべて変数化するのは手間だが、一度やれば保守が楽

### i18n（国際化）
- Figma Community はグローバルなので英語がデフォルト、しかし日本語ユーザーにも配慮
- `data-i18n` 属性 + `t()` 関数の組み合わせはシンプルで実装コスト低い
- 動的テキスト（プログレスメッセージ等）は `{param}` プレースホルダーで対応
- localStorage で言語設定を永続化（Figma プラグイン iframe では利用可能）
- HTMLのデフォルトテキストとTEXTSオブジェクトの不整合に注意: 英語モードではHTMLのデフォルトがそのまま表示される

## 記事執筆

### 実用ガイド vs 技術体験記
- 旧記事はピクセル比較アルゴリズムと BYOK パターンが主役だったが、読者が求めるのは「このプラグインで何ができて、どう使うか」
- 技術的な仕組みは「簡潔に1セクション」に留め、使い方・ユースケース・撮影のコツを厚くした方が実用的
- AI 機能の導入→撤退ストーリーは「ミニマル化の判断プロセス」として1セクションに凝縮すると読者にも学びがある

### プラグインの変更と記事の乖離
- v3.0.0（単一フロー化）と v4.0.0（AI 削除）で記事の前提が大きく変わった
- 公開前に記事を完全に書き直す判断は正しかった（部分修正では整合性が取れない）
- 記事内のプラグイン ID・URL は manifest.json と必ず同期させること

### note と Medium の書き分け
- note: PREP 法、冒頭3行で「何/誰向け/何が得られるか」、引用ブロックで心の声、300-500文字に1枚のビジュアル
- Medium: scene-setting opening、一文25ワード以内、em dash 回避、エッセイ的な締め、キャプション付きビジュアル
- note を先に書き、それをベースに Medium 版を英語圏向けに再構成するフローが効率的

## ビルド構成

### 単一 HTML ファイルの限界
- Figma プラグイン UI は `ui.html` として単一ファイルでロードされる制約がある
- 1000行超の単一ファイルは保守性が低い
- esbuild で CSS + TypeScript → 単一 HTML にバンドルする構成で、開発時は分割・リリース時は結合が実現可能
- IIFE バンドルの場合、HTML インラインハンドラ（onclick等）から呼ぶ関数は `(window as any).fn = fn` で明示的に公開が必要

### minify の適用範囲
- ui.html（JS部分）と code.js の両方に minify を適用すべき
- watch モードでは minify 無効にしてデバッグしやすくする（`minify: !isWatch`）
